[package]
name = "server"
version = "0.1.0"
edition = "2021"

[dependencies]
common = { path = "../common" }
engine = { path = "../engine" }
serde = { version = "1.0", features = ["derive"] }
bincode = "1.3"
