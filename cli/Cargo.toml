[package]
name = "cli"
version = "0.1.0"
edition = "2021"

[dependencies]
common = { path = "../common" }
server = { path = "../server" }
clap = { version = "4.0", features = ["derive"] }
bincode = "1.3"
